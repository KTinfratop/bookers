<h1>Books</h1>

<% @books.each do |book| %>
  <p>title</p>
  <span>
    <%= link_to book.title, "/books/#{book.id}" %>
  </span>
  
  <td><a>Show</a></td>
  <td><a>Edit</a></td>
  <td><a>Destroy</a></td>
  
<% end %>