<div class= "show">
  
  <div class= "show_title">
    <h4>Title:</h4>
    <p><%= @book.title %></p>
  </div>

  <div class= "show_body">
    <h4>Body:</h4>
    <p><%= @book.body %></p>
  </div>
  
</div>

<%= link_to "Edit", edit_list_path(@book.id) %>
<%= link_to "Back", books_path(@book.id) %>