<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <div class="edit_submit">
  <%= f.submit 'Update Book' %>
  </div>
  
  <a>Show</a>
<a>Back</a>
  
<% end %>